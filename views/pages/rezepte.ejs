<!DOCTYPE html>
<html>

<head>
  <% include ../partials/header %>
</head>

<body>
  <%- include ../partials/navbar %>

  <div class="flex-container">

    <div class="selection-items">
      <table class="table list-recipe-selection">
        <thead>
          <tr>
            <th scope="col">Auswahl</th>
          </tr>
        </thead>
        <tbody>
            <% recipes.forEach(r => { %>
              <tr id="<%= r.name %>" class="select-recipe">
                <%= r.name %>
              </tr>
              <% }) %>
        </tbody>
      </table>
    </div>

    <%- include ../partials/recipe %>
  </div>

</body>

</html>